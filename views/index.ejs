
<!DOCTYPE html>

<html>
 <head>
    <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 100%;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
<body>
 <div id="map"></div>
    <script>
      var map;
      function initMap() {
        map = new google.maps.Map(document.getElementById('map'), {
          center: {lat: 37.0902, lng: -95.7129},
          zoom: 4
        }); /*
        var DesMoines = {lat: 41.6195, lng: -93.598};
        var homeMarker = new google.maps.Marker({
          position: DesMoines,
          map: map,
          title: "My pin for Des Moines"
        }); */
      }
    </script>
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC9KxRvcZhsLFBf7ggjZN4zO1lOJCUoMy4&callback=initMap" 
    async defer></script>


<script>
function dropPins() {
  $.ajax({
          url: "/getSessionData",
          type: 'POST',
          success:function(data){
            console.log("Checking if logged in...");
            if(data.loggedIn) {
              console.log("Logged in, displaying map markers.");
              $.ajax({
                url: "/getResouces",
                type: "POST",
                success:function(data) {
                  if(data.moreData){
                    //Add pin dropping functions here
                    var address = data.moreData;
                    var geocoder = new google.maps.Geocoder();
                    geocoder.geocoder({address: address}, function(results, status){
                      if(status == google.maps.GeocoderStatus.OK) {
                        
                      }
                    })
                    dropPins();
                  }
                },
                error:function(xhr, status, error) {
                  console.log("error");
                }
              });
            }
          },
          error: function(xhr,status,error){
            console.log("error");
          }
        });
}
function selectHome(){
          $("#RBL").removeClass("active");
          $("#ABL").removeClass("active");
          $("#HBL").addClass("active");
          console.log("Making About Active");
        }
        selectHome();
</script>
</body>
</html>

